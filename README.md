# vierpunkteins_kampf

**[v1.0.1]** Owlbear-Rodeo-Erweiterung: Kampfliste und Initiative für Charakter-Tokens (CHARACTER).

Build: `npm install` und `npm run build`; die Extension über das generierte `dist`-Verzeichnis bzw. nach Owlbear-Rodeo-Vorgaben einbinden.
